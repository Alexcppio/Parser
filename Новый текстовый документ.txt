#include <iostream>
#include <algorithm>
#include <iterator>
#include <vector>
#include <ctime>
#include <list>

using namespace std;
using std::vector;

void summListVect(vector<int> &myVector, list<int> &myList)
{
	vector<int> summVector(100);
	vector<int>::iterator summIt;

	vector<int>::iterator itMyVector;
	itMyVector = myVector.begin();
	list<int>::iterator itMyList;
	itMyList = myList.begin();

	cout << "SummVector\n";
	for (summIt = myVector.begin(); summIt != myVector.end(); ++summIt)
	{
		*summIt = myVector[i++] + *itMyList++;
		cout << *summIt << " ";
	}
	cout << endl;
}

int main()
{
	srand(time(0));


	vector<int> myVector(100);
	vector<int>::iterator it;

	cout << "Vector\n";
	for (it = myVector.begin(); it != myVector.end(); ++it)
	{
		*it += 1 + rand() % 10;
		cout << *it << " ";
	}
	cout << endl;

	list<int> myList(100);
	list<int>::iterator itList;

	cout << "List\n";

	itList = myList.begin();
	for (int n : myList)
	{
		*itList = 1 + rand() % 10;
		cout << *itList << " ";
	}
	cout << endl;

	summListVect(myVector, myList);

	return 0;
}